import { ThemeConfigPopup } from './types/popup';

/**
 * Popup 组件
 */
export const popupConfig: ThemeConfigPopup = {
  popupWrap: {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      bottom: 0,
      borderTopLeftRadius: 15,
      borderTopRightRadius: 15,
    },
  },
};
